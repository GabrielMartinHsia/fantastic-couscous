#include <iostream>
#include <vector>
#include <string>
#include <algorithm> //std::find
#include <map>

// std::map<std::string, int> WordHistogram(std::string &s){
//     for (char c : s){
//         int c_count = 0;
//         auto it = std::find(s.begin(), s.end(), c);
//         while (it != s.end()){
//             c_count++;
//         }
//         std::pair<std::string, int> c_pair = (c, c_count); 
//     }
// }

std::map<std::string, int> WordHistogram(std::string &s) {
    
}

int main()
{
    // std::vector<int> v = {12, -2, 0, 4, 13, 3, 5, 4};
    // auto it = std::find(v.begin(), v.end(), 4);
    // if (it != v.end())
    // {
    //     const auto &n = *it;
    //     std::cout << "Found n: " << n << std::endl;
    // }
    // else
    // {
    //     std::cout << "Didn't find." << std::endl;
    // }

    // std::string s = "well hello there.";
    // for (char c : s){
    //     std::cout<<c<<std::endl;
    // }
}
